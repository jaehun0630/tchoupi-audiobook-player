services:
  tchoupi-web:
    image: nginx:alpine
    ports:
      - "${TCHOUPI_HTTP_PORT:-8080}:80"
    volumes:
      - ./html:/usr/share/nginx/html:ro
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
